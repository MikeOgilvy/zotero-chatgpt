# S4 chat-settings and native-interaction verification

Date: 2026-09-09. Scope: catalog composer, conversation history, shareable diagnostics, sidebar width clamp, ten toolbar toggle cycles, KaTeX stylesheet, native-ish chrome, zoom/page matrix, window sizes, notes-pane switching, four-edge selection bars, and Ask-in-sidechat without sending. Model send combinations (A23/A24) were not run.

## Artifact and environment

- XPI: `dist/zotero-codex-reader-0.3.0a1-dev.xpi`; npm workspace version `0.3.0-alpha.1`.
- SHA-256: `c1b898ac4e35de49a65c96ce56e08db6949071b62618ff7c34a01672b942a0db` (about 98 MB; `dist/SHA256SUMS` matches). Product XPI contents were **not** regenerated this session; host drivers live in the separate `zcr-host-test@local` addon.
- `npm run verify:artifacts` on that XPI: 76 files, sibling SHA256SUMS checked, no quoted `node:` imports in the production JS, no auth/paper records, required licenses present.
- Actual host: Zotero 9.0.6 on macOS Apple Silicon. Node 24 for build and tests only.
- Profile and data: dedicated ignored `.zcr-dev/profile` and `.zcr-dev/data`, synthetic PDF only. The regular Zotero profile (`mi2zhr2s.default`), library and other Codex sessions were not used.
- The dedicated profile was already signed in through the official browser flow. No authentication file was read, copied or logged. The driver did not send a model request.

## Automated checks

`npm run typecheck`, `npm run lint` and `npm run test:unit` pass (**215 tests / 28 files** after this session’s S6 script tests). These use doubles; they are not host evidence.

## Dedicated host run

`node scripts/prepare-host-test.mjs --s4` installed the existing XPI and the extended S4 driver. Result `passed`. **40/40 executed checks passed; 5 NOT RUN.** No Codex turn was submitted.

| Actual host behavior | Result |
| --- | --- |
| Isolated data directory; bundled XPI `0.3.0a1` with hash `c1b898ac…` loaded | PASS |
| Native runtime handshake ready; KaTeX stylesheet loaded | PASS |
| Model / speed / effort composer present; catalog loaded (5 models); speed and effort independent (2 / 7 options) | PASS |
| Sidebar width clamped to 560px when the stored pref was 900 | PASS |
| Conversation history lists the current conversation; 新对话 available | PASS |
| 复制诊断 available; status confirms the whitelist copy (no paper text, account or path) | PASS |
| Toolbar toggle ten open/close cycles return to the starting pressed state | PASS |
| Real PDF selection shows the native popup and the plugin bar inside the view without covering the popup | PASS |
| Chat buttons share the host system font (`-apple-system` / `system-ui`); no vivid brand chrome | PASS |
| Composer settings remain readable | PASS |
| A26 live dark/light theme switch | NOT RUN (`extensions.zotero.theme` was not readable on this host; colors were not toggled) |
| A27 `auto` / `page-fit` / `page-width` open+close keep page and mode | PASS |
| A27 fixed 1.25 scale: open keeps page, close restores numeric scale | PASS |
| A27 manual zoom while open wins on close | PASS |
| A27 close after navigating to page 2 keeps page 2 | PASS |
| A28 800 CSS px window | NOT RUN (`resizeTo` left `innerWidth`/`outerWidth` at 927; macOS would not shrink further) |
| A28 1024 CSS px: docked pane 357px, reader inner 667, no overlay, composer usable | PASS |
| A28 1440 CSS px: docked pane 542px, reader inner 898, no overlay, composer usable | PASS |
| Collections pane present without covering chat | PASS |
| Native notes mode releases the chat dock; Codex reopens afterward | PASS |
| A30 top / left / right selection bar stays in view and does not cover the native popup | PASS |
| A30 bottom-edge selection | NOT RUN (after scrolling to the bottom, no visible text-layer spans remained in view on this two-page synthetic PDF) |
| A29 native color popup intact before Ask; Ask adds a draft citation without sending | PASS |
| A23/A24 send with changed model/speed/effort | NOT RUN (driver does not send; quota until 2026-09-15) |

No answer text was faked. Upstream usage-limit checks remain those recorded in [S2](s2.md) and [S3](s3.md).

## Boundaries and next stage

This remains a **development preview**. Process-kill restart QA is recorded in [S5](s5.md). Re-run `node scripts/prepare-host-test.mjs --s4` after packaging. Leave the dedicated Zotero window untouched while a host run is in progress.
