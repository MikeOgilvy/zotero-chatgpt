# S5 process-kill and addon-reload verification

Date: 2026-09-09. Scope: dedicated-profile restart QA that does **not** send a live model turn. Handshake after kill/reload, conversation identity, and an injected uncertain leftover. In-flight `turn/start` resume remains NOT RUN because of the ChatGPT Codex usage limit (resets 2026-09-15).

## Artifact and environment

- XPI: `dist/zotero-codex-reader-0.3.0a1-dev.xpi` (not regenerated this session).
- SHA-256: `c1b898ac4e35de49a65c96ce56e08db6949071b62618ff7c34a01672b942a0db`.
- Host: Zotero 9.0.6 on macOS Apple Silicon. Isolated `.zcr-dev/profile` and `.zcr-dev/data` only. Regular profile `mi2zhr2s.default` was left running and was not used.
- Signed-in on the dedicated profile via the official flow. No authentication file was read, copied or logged.
- Driver: `node scripts/prepare-host-test.mjs --s5`. No `turn/start`.

## Dedicated host run

Result `passed`. **17/17 executed checks passed; 1 NOT RUN.**

| Actual host behavior | Result |
| --- | --- |
| Isolated data directory; bundled XPI loaded; runtime handshake ready | PASS |
| Exactly one owned `app-server` whose args include this profile path | PASS |
| Not generating before the kill | PASS |
| `kill -TERM` of that PID: the killed PID disappeared; a replacement PID appeared under the same profile | PASS (replacement `72595` after killed `72575`) |
| UI surfaced `error` after the kill (`Codex connection ended.`) | PASS |
| Reopen sidebar recovered handshake to `ready`; still one owned process; not generating | PASS |
| Same conversation id after the kill/reopen | PASS |
| In-flight turn resume after killing a running request | NOT RUN (quota; no live `turn/start` was started) |
| Disable add-on stops the owned process | PASS |
| Re-enable handshake ready; same conversation; not generating | PASS |
| While disabled, inject an `uncertain` leftover (fake `threadId`, no live turn); after enable, isolation copy is shown and generating stays false | PASS |

The leftover is a driver-written conversation fixture, not a model answer. Resume of that fake thread is expected to fail; the request stayed `uncertain` and the sidebar showed `An earlier request in this conversation could not be confirmed; start a new conversation to continue.` No auto-send was observed.

RPC order `thread/resume` then `thread/read` remains proven by unit tests (`tests/core/recovery.test.ts`). This host run proves kill/reload handshake and uncertain isolation without inventing a terminal state.

## Limits

- Cannot host-test resume of a **running** turn until quota allows a real `turn/start`.
- The first TERM attempt in an earlier driver revision asserted “process count == 0” and failed because a replacement PID appeared immediately; the passing driver asserts the killed PID is gone and allows at most one replacement.
