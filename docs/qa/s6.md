# S6 clean-environment install and upgrade verification

Date: 2026-09-09. Scope: local-only install layout, Node upgrade/rollback CLI, a **worktree-copy rebuild** (not `git HEAD`), virgin-profile AddonManager install, and a **two-version local host upgrade/rollback**. No GitHub Release was downloaded. No model request was sent. No LICENSE was invented.

## Artifact and environment

- Original-tree product XPI left in place: `dist/zotero-codex-reader-0.3.0a1-dev.xpi`.
- SHA-256: `c1b898ac4e35de49a65c96ce56e08db6949071b62618ff7c34a01672b942a0db`.
- Worktree-copy rebuild at `/tmp/zcr-clean-rebuild` reproduced the same a1 digest. A version-bumped a2 XPI was packaged **only** in that temp copy and copied into gitignored `.zcr-dev/s6-upgrade/artifacts/` (`445f47243bb4108a0fc73c0e7c179702cc5c1068bbefb38b96d2a2a43c7b0309`). The original `packages/zotero/manifest.json` remains `0.3.0a1`.
- Node 24 for build/tests and the layout CLI. This machine has Node and Codex CLI, so it is **not** a Node-free user environment.
- Host: Zotero 9.0.6, `-no-remote`. Regular profile `mi2zhr2s.default` was left running and was not used. Signed-in `.zcr-dev/profile` / `.zcr-dev/data` were not written.

## Node layout CLI

`npm run verify:install` wraps `node scripts/install-lifecycle.mjs`.

| Check | Result | Limit |
| --- | --- | --- |
| Refuse regular Zotero profile paths and signed-in `.zcr-dev/profile` / `.zcr-dev/data` | PASS (unit) | `.zcr-dev/s6-virgin/`, `.zcr-dev/s6-upgrade/`, and `.zcr-dev/clean/` are allowed |
| Prepare / upgrade / rollback / extract / `updates.json` / `build-info.json` / `codesign --verify --strict` | PASS (unit) | Fixture XPIs plus two packaged local XPIs |
| `npm run verify:artifacts` | PASS (76 files; SHA256SUMS `c1b898ac…`) | Worktree copy, not a clean `git HEAD` checkout |

## Clean worktree rebuild

Ran in `/tmp/zcr-clean-rebuild` after `rsync` of the current working tree (excluded `.zcr-dev`, `node_modules`, `dist`, `.git`, `.worktrees`, `build`, `coverage`) plus a copy of `.zcr-dev/runtime-cache/`. Then `npm ci`, `npm run package:dev`, `npm run verify:artifacts`.

The rebuilt a1 XPI matched `c1b898ac…`. The original `dist/` was not overwritten. A second XPI (`0.3.0a2`) was produced in the temp copy only by bumping that copy’s Zotero manifest.

This is **not** a clean git-HEAD checkout rebuild: commit is forbidden, so `HEAD` is not this working tree.

## Dedicated `--s6` virgin host

`node scripts/prepare-host-test.mjs --s6` then Zotero `-no-remote` on `.zcr-dev/s6-virgin/`. Result `passed`. **15/15 executed checks passed; 2 NOT RUN** in the earlier session (live send; version-bump because only one XPI existed then). Account state `signedOut`. Artifact hash `c1b898ac…`.

## Dedicated `--s6` two-version host

```sh
node scripts/prepare-host-test.mjs --s6 \
  --rollback-xpi .zcr-dev/s6-upgrade/artifacts/zotero-codex-reader-0.3.0a1-dev.xpi \
  --upgrade-xpi .zcr-dev/s6-upgrade/artifacts/zotero-codex-reader-0.3.0a2-dev.xpi
/Applications/Zotero.app/Contents/MacOS/zotero \
  -no-remote \
  -profile "$PWD/.zcr-dev/s6-upgrade/profile" \
  -datadir "$PWD/.zcr-dev/s6-upgrade/data"
```

Result `passed` in `.zcr-dev/s6-upgrade/host-report.json`. **19/19 executed checks passed; 2 NOT RUN** (live send; same-version staging copy is skipped when AddonManager installs a newer XPI). First file-copy attempt left AddonManager at `0.3.0a1`; the passing run uninstalls then `getInstallForFile` for upgrade and rollback.

| Actual host behavior | Result |
| --- | --- |
| Isolated data directory `.zcr-dev/s6-upgrade/data` | PASS |
| Profile is `.zcr-dev/s6-upgrade/profile`, not the signed-in tree | PASS |
| AddonManager loaded bundled XPI `0.3.0a1` | PASS |
| Native runtime handshake ready; one owned `app-server` | PASS |
| Auth `signedOut`; login control visible; not generating | PASS |
| Live model send | NOT RUN (quota; driver does not send) |
| Seeded a schemaVersion 1 conversation JSON | PASS |
| Disable stops the owned process | PASS |
| Same-version XPI staging copy | NOT RUN (two-version path uses AddonManager install) |
| Version-bump upgrade to packaged `0.3.0a2`; handshake; still signed out; records kept | PASS |
| Rollback to packaged `0.3.0a1`; handshake; still signed out; records kept | PASS |

No answer text was faked. Credentials were not copied from the signed-in profile.

## Not run (true leftovers)

- Install from an actual GitHub Release download
- Machine without system Node/Codex CLI
- Download-origin quarantine / Gatekeeper
- A01–A30 re-execution on a public release candidate
- Live Codex streaming (quota until 2026-09-15)
- Clean `git HEAD` checkout rebuild (commit is forbidden)
- LICENSE choice and public `gh release`
